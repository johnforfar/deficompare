import {TokenMetricDB} from '../../sampleData';

export class TokenMetricModel {
  private model: TokenMetricDB;
  constructor(model:TokenMetricDB) {
    this.model = model;
  }
  getGasFeeData(){
  }
}
