import React from "react";
import "./styles.scss";

type ContainerProps = {};

const ComponentTemplate = (props: ContainerProps) => {
  return <div className="container"></div>;
};

export default ComponentTemplate;
